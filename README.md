# Newzora Backend

Backend service for **Newzora** – A SaaS news + live debate platform.

## 🚀 Tech Stack

- Node.js
- Express.js
- MongoDB Atlas
- Mongoose
- JWT Authentication (in progress)
- OAuth (Google & GitHub – upcoming)

## 📁 Project Structure

backend/
│── config/
│── controllers/
│── models/
│── routes/
│── services/
│── middleware/
│── server.js
│── .env

## ⚙️ Setup Instructions

1. Clone the repository
2. Navigate to backend folder
3. Install dependencies:
npm install

4. Create a `.env` file with:
PORT=5000
MONGO_URI=your_mongodb_connection_string
JWT_SECRET=your_secret_key

5. Start the server:
node server.js


## 📌 Current Status

✔ Express server setup  
✔ MongoDB Atlas connection  
🔄 Authentication system in progress  


